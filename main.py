#!/usr/bin/python3

from board import Board
from player import Player
import pprint

players = [ Player("Damo"),
            Player("Damo"),
            Player("Pingaz")
          ]

board = Board(players)
